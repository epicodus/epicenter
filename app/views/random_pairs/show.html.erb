<h1>Random Pairs</h1>
<% if @random_pairs %>
  <h3>Try pairing with one of these students today:</h3>
  <% @random_pairs.each do |pair| %>
    <p><%= pair.name %></p>
  <% end %>
<% else %>
  No random pair available just yet!
<% end %>
