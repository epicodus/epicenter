<div class="navbar_style">
  <nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <%= link_to "Epicenter", student_path(current_user), class:"navbar-brand" %>
      </div>

      <div class="navbar-brand">|</div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <%= link_to "#{current_user.name}", student_path(current_user.id), class:"navbar-brand" %>
        <%= link_to "Assessments", assessments_path, class:"navbar-brand" %>
        <ul class="navbar-brand">
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Analytics <span class="caret"></span></a>
            <ul class="dropdown-menu" role="menu">
              <li><%= link_to "Overview", root_url %></li>
              <li><%= link_to "Student Analytics", root_url %></li>
              <li><%= link_to "Assessment Analytics", root_url %></li>
            </ul>
          </li>
        </ul>
      </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
  </nav>
</div>
