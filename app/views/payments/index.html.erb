<h1>Your payments</h1>

<ul>
  <% @payments.each do |payment| %>
    <li>
      <%= number_to_currency(payment.amount / 100) %>
      paid on
      <%= payment.created_at.strftime("%F") %>
    </li>
  <% end %>
</ul>

<h2>Make a payment</h2>

<p><%= link_to "Make upfront payment", new_upfront_payment_path %></p>
<p><%= link_to "Start recurring payments", new_recurring_payment_path %></p>

<p>
  If you have any questions or need to stop your payments, contact Michael.
</p>
