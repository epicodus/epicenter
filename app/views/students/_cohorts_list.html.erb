<%= f.collection_select :cohort_id, cohorts, :id, :description, { include_blank: "Please select a #{descriptor} cohort" }, { class: 'col-md-4 form-control', id: "enrollment_cohort_id_#{descriptor}" } %>
